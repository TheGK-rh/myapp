<% unless @user == current_user %>
  <div class="my-3" id="follow_form">
    <% if current_user.following?(@user) %>
      <%= render 'devise/shared/unfollow' %>
    <% else %>
      <%= render 'devise/shared/follow' %>
    <% end %>
  </div>
<% end %>
