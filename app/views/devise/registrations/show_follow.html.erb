<div class="container">
  <div class="row">
    <div class="col-6 follow-show">
      <h5><%= @user.name %>：<%= @title %></h5>
      <br>
      <% if @users.any? %>
        <table class="table">
          <thead style="background: dodgerblue;">
            <tr>
              <th scope="col">ID：</th>
              <th scope="col">ユーザーネーム：</th>
            </tr>
          </thead>
          <% @users.each do |user| %>
          <tbody>
            <tr>
              <th scope="row"><%= user.id %></th>
              <td><%= link_to user.name, profile_path(user) %></td>
            </tr>
          </tbody>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
